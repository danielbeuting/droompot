function decodeHtml(v=""){return String(v??"").replace(/&amp;/g,"&").replace(/&quot;/g,'"').replace(/&#39;/g,"'").replace(/\\u002F/gi,"/").replace(/\\u0026/gi,"&").replace(/\\\//g,"/").replace(/&amp%3B/gi,"&");}
function clean(v=""){return decodeHtml(v).replace(/\s+/g," ").trim();}
function meta(html,key){for(const p of [new RegExp(`<meta[^>]+(?:property|name|itemprop)=["']${key}["'][^>]+content=["']([^"']+)["']`,"i"),new RegExp(`<meta[^>]+content=["']([^"']+)["'][^>]+(?:property|name|itemprop)=["']${key}["']`,"i")]){const m=html.match(p);if(m?.[1])return clean(m[1]);}return "";}
function price(v){const s=clean(v);let m=s.match(/(?:€|EUR|\$|USD|£|GBP)?\s*([0-9]{1,7}(?:[.,][0-9]{1,2})?)/i);if(!m)return "";let x=m[1];if(x.includes(",")&&x.includes(".")){x=x.lastIndexOf(",")>x.lastIndexOf(".")?x.replace(/\./g,"").replace(",","."):x.replace(/,/g,"");}else x=x.replace(",",".");const n=Number(x);return Number.isFinite(n)?n.toFixed(2):"";}
function walk(x,out=[]){if(Array.isArray(x))x.forEach(v=>walk(v,out));else if(x&&typeof x==="object"){out.push(x);Object.values(x).forEach(v=>walk(v,out));}return out;}
function ldProduct(html){for(const m of html.matchAll(/<script[^>]+type=["']application\/ld\+json["'][^>]*>([\s\S]*?)<\/script>/gi)){try{const parsed=JSON.parse(decodeHtml(m[1]).trim().replace(/;$/,"").replace(/&quot;/g,'"'));for(const o of walk(parsed)){const t=Array.isArray(o?.["@type"])?o["@type"]:[o?.["@type"]];if(t.map(v=>String(v).toLowerCase()).includes("product")){let image=o.image;if(Array.isArray(image))image=image[0];if(image&&typeof image==="object")image=image.url||image.contentUrl||"";let offers=Array.isArray(o.offers)?o.offers[0]:o.offers;return {title:clean(o.name),image:clean(image),price:price(offers?.price||offers?.lowPrice||offers?.highPrice||offers?.priceSpecification?.price)};}}}catch{}}return {};}
function openGraph(html){return {title:meta(html,"og:title")||meta(html,"twitter:title")||clean(html.match(/<title[^>]*>(.*?)<\/title>/is)?.[1]),image:meta(html,"og:image")||meta(html,"twitter:image"),price:price(meta(html,"product:price:amount")||meta(html,"og:price:amount")||meta(html,"price"))};}
function merge(a,b){return {title:a?.title||b?.title||"",price:a?.price||b?.price||"",image:a?.image||b?.image||""};}
function embedded(html){const titleKeys=["productTitle","productName","displayName","name","title"];const priceKeys=["sellingPrice","currentPrice","salesPrice","price","value"];const imageKeys=["imageUrl","primaryImage","image","largeImageUrl","zoomImageUrl","src"];let title="",p="",image="";for(const k of titleKeys){const m=html.match(new RegExp(`"${k}"\\s*:\\s*"([^"]{3,400})"`,`i`));if(m){const c=clean(m[1]);if(c&&!['bol','bol.com','hema','hema.nl','intertoys'].includes(c.toLowerCase())){title=c;break;}}}for(const k of priceKeys){for(const m of html.matchAll(new RegExp(`"${k}"\\s*:\\s*"?([0-9]+(?:[.,][0-9]{1,2})?)"?`,`ig`))){const c=price(m[1]);if(c&&c!=="0.00"){p=c;break;}}if(p)break;}for(const k of imageKeys){const m=html.match(new RegExp(`"${k}"\\s*:\\s*"(https?:[^\"]+)"`,`i`));if(m){const candidate=decodeHtml(m[1]);if(/^https?:\/\//i.test(candidate)){image=candidate;break;}}}return {title,price:p,image};}
function amazon(html){const text=id=>clean(html.match(new RegExp(`<[^>]+id=["']${id}["'][^>]*>([\\s\\S]*?)<\\/[^>]+>`,`i`))?.[1]?.replace(/<[^>]+>/g," "));const attr=(id,name)=>clean(html.match(new RegExp(`<[^>]+id=["']${id}["'][^>]*${name}=["']([^"']+)["']`,`i`))?.[1]);let p="";for(const id of ["priceblock_ourprice","priceblock_dealprice","corePrice_feature_div","corePriceDisplay_desktop_feature_div","apex_desktop"]){p=price(text(id));if(p)break;}if(!p){const w=clean(html.match(/class=["'][^"']*a-price-whole[^"']*["'][^>]*>([^<]+)/i)?.[1]);const f=clean(html.match(/class=["'][^"']*a-price-fraction[^"']*["'][^>]*>([^<]+)/i)?.[1]);if(w)p=price(w+(f?`.`+f:""));}return {title:text("productTitle"),image:attr("landingImage","data-old-hires")||attr("landingImage","src"),price:p};}
function titleFromUrl(url){try{const parts=new URL(url).pathname.split('/').filter(Boolean);const pIndex=parts.indexOf('p');let slug=(pIndex>=0?parts[pIndex+1]:parts.at(-2))||"";slug=decodeURIComponent(slug).replace(/[-_]+/g,' ').replace(/\b\d{6,}\b/g,' ').replace(/\s+/g,' ').trim();if(!slug)return "";return slug.split(' ').map(w=>/^[A-Z0-9]{2,}$/.test(w)?w:w.charAt(0).toUpperCase()+w.slice(1)).join(' ');}catch{return "";}}
function firstBolImage(text=""){const decoded=decodeHtml(text);const patterns=[/https?:\/\/media\.s-bol\.com\/[A-Za-z0-9_./?=&%+~-]+/ig,/https?:\/\/[^\s"'<>]+bol\.com[^\s"'<>]+\.(?:jpg|jpeg|png|webp)(?:\?[^\s"'<>]*)?/ig];for(const rx of patterns){const matches=[...decoded.matchAll(rx)].map(m=>m[0].replace(/[),.;]+$/,''));const preferred=matches.find(u=>/media\.s-bol\.com/i.test(u)&&!/(logo|icon|sprite|avatar|placeholder)/i.test(u));if(preferred)return preferred; if(matches[0])return matches[0];}return "";}
function bolTitle(html,url){const candidates=[meta(html,"og:title"),meta(html,"twitter:title"),clean(html.match(/<h1[^>]*>([\s\S]*?)<\/h1>/i)?.[1]?.replace(/<[^>]+>/g,' ')),clean(html.match(/"productTitle"\s*:\s*"([^"]{3,400})"/i)?.[1]),clean(html.match(/"name"\s*:\s*"([^"]{3,400})"/i)?.[1]),titleFromUrl(url)];for(let t of candidates){t=clean(t).replace(/\s*\|\s*bol(?:\.com)?.*$/i,'').replace(/\s*-\s*bol(?:\.com)?.*$/i,'');if(t&&!/^bol(?:\.com)?$/i.test(t)&&t.length>2)return t;}return "";}
async function fetchText(url,{timeout=15000,headers={}}={}){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),timeout);try{const r=await fetch(url,{redirect:"follow",signal:controller.signal,headers});if(!r.ok)throw new Error(`HTTP ${r.status}`);return {text:await r.text(),url:r.url};}finally{clearTimeout(timer);}}
async function fetchPage(url){return fetchText(url,{timeout:12000,headers:{"User-Agent":"Mozilla/5.0 (iPhone; CPU iPhone OS 18_0 like Mac OS X) AppleWebKit/605.1.15 Version/18.0 Mobile/15E148 Safari/604.1","Accept":"text/html,application/xhtml+xml","Accept-Language":"nl-NL,nl;q=0.9,en;q=0.8","Cache-Control":"no-cache","Referer":"https://www.google.com/"}});}
function bolFromReader(markdown,url){const text=String(markdown||"");const lines=text.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);let title="";for(const line of lines){const m=line.match(/^#\s+(.{3,250})$/);if(m&&!/bol\.com|bol$/i.test(m[1])){title=clean(m[1]);break;}}if(!title){const m=text.match(/^Title:\s*(.+)$/mi);if(m)title=clean(m[1]).replace(/\s*\|\s*bol.*$/i,"");}if(!title)title=titleFromUrl(url);let p="";for(const rx of [/De prijs van dit product is\s*['“]?([0-9]+)['”]?\s*euro(?:\s*en\s*['“]?([0-9]{1,2})['”]?\s*cent)?/i,/Prijsinformatie en bestellen[\s\S]{0,700}?([0-9]{1,5}[,.][0-9]{2})/i,/\b€\s*([0-9]{1,5}(?:[,.][0-9]{2})?)/i,/\b([0-9]{1,5}[,.][0-9]{2})\b/]){const m=text.match(rx);if(m){p=m[2]!=null?`${m[1]}.${String(m[2]).padStart(2,'0')}`:price(m[1]);if(p)break;}}let image=firstBolImage(text);if(!image){const all=[...text.matchAll(/!\[[^\]]*\]\((https?:\/\/[^)\s]+)\)/g)].map(m=>m[1]);image=all.find(x=>/media\.s-bol\.com/i.test(x))||all.find(x=>/bol\.com/i.test(x))||"";}return {title,price:p,image};}
async function fetchBolReader(url){const reader=`https://r.jina.ai/${url}`;const {text}=await fetchText(reader,{timeout:20000,headers:{"Accept":"text/plain","X-Return-Format":"markdown"}});return bolFromReader(text,url);}

export default async(req)=>{const u=new URL(req.url).searchParams.get("url");if(!u||!/^https?:\/\//i.test(u))return Response.json({error:"Invalid URL"},{status:400});let input;try{input=new URL(u);}catch{return Response.json({error:"Invalid URL"},{status:400});}const originalHost=input.hostname.toLowerCase();const isBol=originalHost.includes("bol.com");let direct={},finalUrl=u,directError="";try{const {text:html,url}=await fetchPage(u);finalUrl=url;const host=new URL(url).hostname.toLowerCase();direct=merge(ldProduct(html),openGraph(html));direct=merge(direct,embedded(html));if(isBol){direct.title=direct.title||bolTitle(html,url);direct.image=direct.image||firstBolImage(html);}if(host.includes("amazon."))direct=merge(direct,amazon(html));}catch(e){directError=e?.message||"direct fetch failed";}let data=direct;if(isBol&&(!data.title||!data.price||!data.image)){try{data=merge(data,await fetchBolReader(u));}catch(e){if(!directError)directError=e?.message||"bol fallback failed";}}if(isBol){data.title=data.title||titleFromUrl(u);}const host=(()=>{try{return new URL(finalUrl).hostname.toLowerCase();}catch{return originalHost;}})();const source=isBol?"bol":host.includes("amazon.")?"amazon":host.includes("intertoys.")?"intertoys":host.includes("hema.")?"hema":"generic";if(!data.title&&!data.price&&!data.image)return Response.json({error:`Productgegevens konden niet worden opgehaald${directError?`: ${directError}`:""}`},{status:502});return Response.json({...data,source,domain:host});};
